# 🧩 Java & Spring Interview Notes

---

## 1. Принципы и основы

### SOLID
Пять принципов проектирования:
- **SRP (Single Responsibility Principle)** — один класс решает одну задачу.
- **OCP (Open/Closed Principle)** — открыт для расширения, закрыт для модификации.
- **LSP (Liskov Substitution Principle)** — наследники должны полностью заменять родителя.
- **ISP (Interface Segregation Principle)** — много специализированных интерфейсов вместо одного общего.
- **DIP (Dependency Inversion Principle)** — зависимость от абстракций, а не от реализаций.

> ⚠️ Нарушение LSP: подкласс изменяет ожидаемое поведение базового класса.

### Принципы ООП
- **Инкапсуляция**
- **Наследование**
- **Полиморфизм**
- **Абстракция**

### Принципы разработки
- **DRY** — не дублируй код.
- **KISS** — пиши просто.
- **YAGNI** — не реализуй лишнего.

### Паттерны проектирования
**Порождающие:** Singleton, Factory Method, Builder  
**Структурные:** Adapter, Decorator, Proxy  
**Поведенческие:** Observer, Strategy, Command

### Big O
Асимптотическая сложность алгоритма (время/память). Используется для оценки производительности.

### Микросервисная архитектура
**Плюсы:** масштабируемость, гибкость, независимые релизы  
**Минусы:** сложность мониторинга, отладки, транзакций

**Паттерны микросервисной архитектуры:**
- API Gateway
- Service Discovery
- Circuit Breaker
- Event Sourcing
- Saga Pattern
- CQRS

---

## 2. Collections (Java Collections Framework)

### Общая структура
Интерфейсы: `Collection`, `List`, `Set`, `Queue`, `Map`

### ArrayList
- Основан на динамическом массиве
- Быстрый доступ по индексу — `O(1)`
- Медленная вставка в середину — `O(n)`

### LinkedList
- Двусвязный список
- Быстрая вставка/удаление — `O(1)`
- Медленный доступ по индексу — `O(n)`

### HashMap
- Основан на массиве бакетов `Node<K,V>[]`
- Коллизии решаются через цепочки или деревья
- Формула индекса: `index = hash(key) & (n - 1)`
- **loadFactor** — порог увеличения массива (по умолчанию 0.75)
- **Вставка:** вычисление хэша, добавление в бакет или обновление значения

### Queue / Stack
- **Stack (LIFO):** последний вошёл — первый вышел
- **Queue (FIFO):** первый вошёл — первый вышел

---

## 3. Исключения

### Checked vs Unchecked
- **Checked:** `IOException`, `SQLException`
- **Unchecked:** `NullPointerException`, `IllegalArgumentException`

### Иерархия
`Throwable → Exception / Error`

---

## 4. Spring / Spring Boot

### Spring Core

- **IoC (Inversion of Control):** контейнер управляет объектами  
- **DI (Dependency Injection):** внедрение зависимостей извне  
- **ApplicationContext:** главный контейнер Spring

### Scope бинов
`singleton`, `prototype`, `request`, `session`, `application`

### Bean
- Создание: XML, Java Config, аннотации
- Инжектирование: `@Autowired`, конструктор, сеттер
- **@Primary** — приоритетный бин
- **@Qualifier** — выбор конкретного бина

### Transaction
Атомарность операций через `@Transactional`

### Подводные камни
`LazyInitializationException` — попытка ленивой загрузки вне сессии

---

### Spring Boot

- **Отличия от Spring:** автонастройка, стартеры, встроенный сервер  
- **Аннотация `@SpringBootApplication`:**
  - `@Configuration`
  - `@EnableAutoConfiguration`
  - `@ComponentScan`

---

## 5. JPA / Hibernate

### Отличие
- **JPA** — спецификация
- **Hibernate** — реализация

### Основные аннотации
`@Entity`, `@Table`, `@Id`, `@GeneratedValue`, `@Column`, `@OneToMany`, `@ManyToOne`

### Требования к Entity
- Публичный класс
- Безаргументный конструктор
- Поле `@Id`
- (Рекомендуется) `Serializable`

### Состояния сущности
`Transient`, `Persistent`, `Detached`, `Removed`

### Кэширование
- 1-й уровень — Session
- 2-й уровень — SessionFactory

### HQL vs SQL
- **HQL:** ориентирован на объекты  
- **SQL:** напрямую к таблицам

### Подводные камни
- N+1 проблема (решение — fetch join, EntityGraph)
- LazyInitializationException

---

## 6. Базы данных

### ACID
Atomicity, Consistency, Isolation, Durability

### Аномалии
Грязное чтение, неповторяемое чтение, фантомное чтение

### Изоляция
`READ UNCOMMITTED`, `READ COMMITTED`, `REPEATABLE READ`, `SERIALIZABLE`

### Индексы
- Повышают скорость поиска
- **Кластерный индекс:** определяет физический порядок строк

### JOIN
`INNER`, `LEFT`, `RIGHT`, `FULL`, `CROSS`

### WHERE vs HAVING
- `WHERE` — до агрегации  
- `HAVING` — после группировки

### Агрегатные функции
`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`

### DML / DDL / DCL / TCL
| Категория | Примеры | Назначение |
|------------|----------|-------------|
| DML | INSERT, UPDATE, DELETE | Работа с данными |
| DDL | CREATE, ALTER, DROP | Структура БД |
| DCL | GRANT, REVOKE | Права |
| TCL | COMMIT, ROLLBACK | Транзакции |

### Constraints
`PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, `NOT NULL`, `CHECK`

---

## 7. Java 8+

### Stream API
Работа с коллекциями в функциональном стиле  
Операции: `map`, `filter`, `reduce`, `collect`

### Лямбды
Анонимные функции для передачи поведения

### Iterable
Базовый интерфейс коллекций с поддержкой `for-each`

---

## 8. JMS (Messaging)

- **RabbitMQ** — брокер сообщений, реализующий AMQP  
- **Kafka** — распределённая система логов, потоковая передача сообщений  
- **ActiveMQ** — классическая очередь сообщений с поддержкой JMS API  

---

## 9. HTTP, WEB, REST, SOAP

- **HTTP** — протокол передачи данных (методы GET, POST, PUT, DELETE)  
- **REST** — архитектурный стиль (ресурс + HTTP методы + JSON)  
- **SOAP** — протокол на XML с WSDL описанием  
- **WEB** — клиент-серверное взаимодействие по HTTP  

---

## 10. Testing

### Пирамида тестирования
1. **Unit Tests** — быстрые, изолированные  
2. **Integration Tests** — взаимодействие модулей  
3. **UI / E2E Tests** — сценарии пользователя

---

## 11. Git

- **merge** — объединяет ветки, сохраняя историю  
- **rebase** — делает историю линейной  
- **reset** — откат (изменяет историю)  
- **revert** — создаёт коммит-откат  
- **squash** — объединяет коммиты  
- **cherry-pick** — перенос выбранного коммита

---

## 12. DevOps

- CI/CD — автоматизация сборки, тестирования, доставки  
- Docker — контейнеризация приложений  
- Kubernetes — оркестрация контейнеров  
- Jenkins / GitHub Actions — системы сборки  
- Мониторинг — Prometheus, Grafana  
- Логи — ELK (Elasticsearch, Logstash, Kibana)

---
